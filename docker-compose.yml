services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: dqo
      POSTGRES_PASSWORD: dqo
      POSTGRES_DB: observability
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
